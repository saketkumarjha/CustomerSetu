# Used by src/lib/api.ts — must match backend API_KEY in backend/.env
VITE_API_KEY=sk_my_super_secret_key_123

# Leave empty: Vite dev server proxies /api → http://localhost:8000 (vite.config.ts)
VITE_API_BASE_URL=
